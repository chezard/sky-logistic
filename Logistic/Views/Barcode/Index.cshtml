@{
    ViewData["Title"] = "Index";
    List<Barcode> barcodes = Model;
}

<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">BARKODLAR</h4>
                <a asp-action="Create" class="btn btn-primary">Create</a>
                <div class="table-responsive">
                    <table class="data-table  index w-100">
                        <thead>
                            <tr>
                                
                                <th>Id</th>
                                <th>Type</th>
                                <th>Barcode</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Barcode barcode in barcodes)
                            {
                            <tr>
                                <td>
                                    @barcode.Id
                                </td>
                                <td>
                                    @barcode.Type
                                </td>
                                <td>
                                    @barcode.Barkod
                                </td>
                                <td>
                                        <a style="padding:10px" asp-action="Detail" asp-route-id="@barcode.Id" class="btn btn-success">Detail</a>
                                        <a style="padding:10px" asp-action="Update" asp-route-id="@barcode.Id" class="btn btn-warning">Edit</a>
                                        <a style="padding:10px" asp-action="Delete" asp-route-id="@barcode.Id" class="btn btn-danger delete">Delete</a>
                                </td>

                            </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

